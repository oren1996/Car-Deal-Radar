# Copy to .env and fill in your own keys. Never commit a real .env file.
# These match the services used in weeks 6-8 of the official course
# (loaded everywhere with load_dotenv(override=True)).
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
HF_TOKEN=
PUSHOVER_USER=
PUSHOVER_TOKEN=
